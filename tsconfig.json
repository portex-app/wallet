{
  // 📌 Portex Wallet SDK 主配置文件
  // 仅用于管理项目引用，不包含具体编译配置

  // 📁 项目引用
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }],

  // 空的文件包含，因为使用引用模式
  "files": []
}
